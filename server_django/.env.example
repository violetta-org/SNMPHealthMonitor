# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# MySQL Database (same as existing query-service)
DB_NAME=metrics
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306

# UDP Listener (must match rasberrypi collector config)
UDP_LISTEN_HOST=0.0.0.0
UDP_LISTEN_PORT=9999
